class ProfileControlObjectNew:

    def __init__(self, profile, is_controlled, total_order_month,total_order_all):
        self.id = id
        self.profile = profile
        self.is_controlled = is_controlled
        self.total_order_month = total_order_month
        self.total_order_all = total_order_all
